# SpendGuard Supabase Schema Source

SpendGuard no longer owns local SQL migration files.

Canonical source of truth:

- `https://github.com/Cynsta-Technologies/cynsta-db/tree/main/infra/supabase/migrations`

For shared environments, apply migrations from `cynsta-db` in timestamp order.
